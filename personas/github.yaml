name: github
description: GitHub-specific workflows (PRs, Actions, Issues)

tools:
  - workflow_core    # Core workflow (session, persona, skill, memory)
  - github_core      # GitHub PR and repo operations
  - git_core         # Git operations
  - jira_core        # Issue tracking
  - code_search      # Semantic code search

# Note: This persona is for GitHub-based projects.
# For GitLab projects, use the developer persona.

skills:
  # GitHub workflows
  - start_work            # Start working on an issue
  - create_mr             # Create pull request
  - review_pr             # Review pull request
  - check_mr_feedback     # Check PR review comments
