# DevOps Persona - Slim Configuration
# Minimal tools for resource-constrained environments

name: devops
description: Deployments, K8s (slim - 57 tools)
persona: personas/devops.md

# Slim tool selection - only the essentials
tools:
  - workflow        # 18 tools - Core (memory, persona, session, skill, infra, meta)
  - k8s_basic       # 22 tools - Essential k8s (pods, logs, deployments, events)
  - jira_basic      # 17 tools - Issue tracking

# Total: ~57 tools ✅
# Missing from full: bonfire_basic (10), quay (8), docker (4)
# Use tool_exec() for bonfire/quay/docker operations when needed
