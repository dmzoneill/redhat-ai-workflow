# Developer Persona - Slim Configuration
# Minimal tools for resource-constrained environments

name: developer
description: Coding, PRs (slim - 57 tools)
persona: personas/developer.md

# Slim tool selection - only the essentials
tools:
  - workflow        # 18 tools - Core (memory, persona, session, skill, infra, meta)
  - git_basic       # 22 tools - Git operations only
  - jira_basic      # 17 tools - Issue tracking

# Total: ~57 tools ✅
# Missing from full: gitlab_basic, lint, docker, make
# Use tool_exec() for GitLab/docker/lint operations when needed
