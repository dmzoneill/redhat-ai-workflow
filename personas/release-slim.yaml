# Release Persona - Slim Configuration
# Minimal tools for resource-constrained environments

name: release
description: Releases, Konflux (slim - 65 tools)
persona: personas/release.md

# Slim tool selection - only the essentials
tools:
  - workflow        # 18 tools - Core (memory, persona, session, skill, infra, meta)
  - konflux_basic   # 22 tools - Pipelines, components, snapshots
  - quay            # 8 tools - Container images
  - jira_basic      # 17 tools - Issue tracking

# Total: ~65 tools ✅
# Missing from full: git_basic (22), appinterface (7)
# Use tool_exec() for git/app-interface operations when needed
