# Release Persona - Slim Configuration
# Only essential tools to stay under limit when combined with other personas

name: release
description: Releases and deployments (slim)
persona: personas/release.md

# Slim tool selection - only the essentials
tools:
  - konflux       # 35 tools - Builds and snapshots
  - quay          # 7 tools - Container images

# Total: ~42 tools
