# Slop Bot Persona Configuration
# Code quality analysis - semantic search, linting, pattern detection, auto-fix

# Auto-validated: 2026-02-06
# Use tool_list() to discover available tools

name: slop
description: Code quality analysis - detect AI slop, code smells, and auto-fix high-confidence issues
persona: personas/slop.md

# Tool modules to load (from tool_modules/aa_*)
# Using _core suffix for heavy modules to stay under 80 tool limit
tools:
  - workflow_core   # Core (session, persona, skill, memory)
  - code_search     # Semantic code search (vector embeddings)
  - lint            # Code formatting and linting
  - git_core        # Git operations for diff analysis
  - slop_fixer      # Auto-fix high-confidence findings

# Skills for code quality analysis
skills:
  - find_similar_code    # Find duplicate/similar code
  - review_pr            # Code review patterns
  - slop_scan            # Run slop detection
  - slop_fix             # Auto-fix high-confidence issues
