# Incident Persona - Slim Configuration
# Minimal tools for resource-constrained environments

name: incident
description: Alerts, Logs (slim - 45 tools)
persona: personas/incident.md

# Slim tool selection - only the essentials
tools:
  - workflow          # 18 tools - Core (memory, persona, session, skill, infra, meta)
  - k8s_basic         # 22 tools - Essential k8s (pods, logs, deployments, events)
  - prometheus_basic  # 5 tools - Metrics, alerts

# Total: ~45 tools ✅
# Missing from full: kibana (9), jira_basic (17), alertmanager (7)
# Use tool_exec() for log search and alertmanager when needed
