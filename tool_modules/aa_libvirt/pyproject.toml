[project]
name = "aa_libvirt"
version = "0.1.0"
description = "Libvirt/KVM virtual machine management tools for AI Workflow"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
