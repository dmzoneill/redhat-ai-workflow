[project]
name = "aa_gdrive"
version = "0.1.0"
description = "Google Drive MCP tools for file management and search"
requires-python = ">=3.10"
dependencies = [
    "google-api-python-client",
    "google-auth-httplib2",
    "google-auth-oauthlib",
]

[tool.setuptools]
packages = ["src"]
