[project]
name = "aa_knowledge"
version = "0.1.0"
description = "Project knowledge management tools for AI Workflow"
requires-python = ">=3.11"
dependencies = ["pyyaml"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


