[project]
name = "aa_sqlite"
version = "0.1.0"
description = "SQLite database tools for AI Workflow"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
