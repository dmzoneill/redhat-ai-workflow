[project]
name = "aa-concur"
version = "0.1.0"
description = "Concur expense automation tools"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40.0",
    "pypdf>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]


