[project]
name = "aa_project"
version = "0.1.0"
description = "Project configuration management tools for AI Workflow"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
