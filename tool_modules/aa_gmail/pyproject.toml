[project]
name = "aa_gmail"
version = "0.1.0"
description = "Gmail MCP tools for email management and search"
requires-python = ">=3.10"
dependencies = [
    "google-api-python-client",
    "google-auth-httplib2",
    "google-auth-oauthlib",
]

[tool.setuptools]
packages = ["src"]
