[project]
name = "aa_github"
version = "0.1.0"
description = "GitHub CLI (gh) tools for AI Workflow"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
