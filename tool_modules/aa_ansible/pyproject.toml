[project]
name = "aa_ansible"
version = "0.1.0"
description = "Ansible automation tools for AI Workflow"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
