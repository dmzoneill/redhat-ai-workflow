# Skill: Close Issue
# Marks a Jira issue as Done with a summary of work completed

name: close_issue
description: |
  Close a Jira issue by transitioning it to Done.
  Adds a comment summarizing the work from branch commits.

  Resolves Jira statuses from config.json.
version: "1.2"

inputs:
  - name: issue_key
    type: string
    required: true
    description: "Jira issue key (e.g., AAP-12345)"

  - name: repo
    type: string
    required: false
    default: "."
    description: "Repository path to look for branch/commits"

  - name: add_comment
    type: boolean
    required: false
    default: true
    description: "Whether to add a closing comment with commit summary"

# No hardcoded constants - resolved from config.json

steps:
  # ==================== LOAD DEVELOPER PERSONA ====================
  # This skill uses git_basic, gitlab_basic, and jira_basic tools

  - name: load_developer_persona
    description: "Load developer persona for Git, GitLab, and Jira tools"
    tool: persona_load
    args:
      persona_name: "developer"

  # ==================== KNOWLEDGE INTEGRATION ====================

  - name: get_jira_patterns
    description: "Get Jira workflow patterns from knowledge base"
    tool: knowledge_query
    args:
      project: "automation-analytics-backend"
      persona: "developer"
      section: "gotchas"
    output: jira_knowledge_raw
    on_error: continue

  - name: parse_jira_knowledge
    description: "Parse Jira knowledge for workflow context"
    compute: |
      knowledge_text = str(jira_knowledge_raw) if jira_knowledge_raw else ""

      gotchas = []

      # Extract gotchas related to Jira workflow
      if knowledge_text:
          for line in knowledge_text.split("\n"):
              line_lower = line.lower()
              if any(kw in line_lower for kw in ["jira", "issue", "status", "transition", "done", "close", "workflow"]):
                  gotchas.append(line.strip()[:150])

      result = {
          "has_knowledge": len(gotchas) > 0,
          "gotchas": gotchas[:5],
      }
    output: jira_knowledge
    on_error: continue

  # ==================== PROACTIVE ISSUE DETECTION ====================

  - name: check_jira_known_issues
    description: "Check for known Jira issues before starting"
    tool: check_known_issues
    args:
      tool_name: "jira_transition"
      error_text: ""
    output: jira_known_issues
    on_error: continue

  # ==================== LOAD CONFIG ====================

  - name: load_config
    description: "Load Jira status configuration and resolve GitLab project"
    compute: |
      from scripts.common.config_loader import load_config

      config = load_config()
      jira_statuses = config.get("jira", {}).get("statuses", {})

      # Resolve GitLab project from issue key
      gitlab_project = "automation-analytics/automation-analytics-backend"
      repos = config.get("repositories", {})
      issue_key = inputs.get("issue_key", "")
      if issue_key:
          project_prefix = issue_key.split("-")[0].upper()
          for name, repo_cfg in repos.items():
              if repo_cfg.get("jira_project") == project_prefix:
                  gitlab_project = repo_cfg.get("gitlab", gitlab_project)
                  break

      result = {
          "done_status": jira_statuses.get("done", "Done"),
          "pending_release_status": jira_statuses.get("pending_release", "Pending Release"),
          "jira_url": config.get("jira", {}).get("url", "https://issues.redhat.com"),
          "gitlab_project": gitlab_project,
      }
    output: cfg

  - name: init_autoheal
    description: "Initialize failure tracking"
    compute: |
      result = {"jira_failures": []}
    output: autoheal_state
    on_error: continue

  # ==================== GET ISSUE DETAILS ====================

  # Step 1: Get current issue status
  - name: get_issue
    description: "Fetch current issue details"
    tool: jira_get_issue
    args:
      issue_key: "{{ inputs.issue_key }}"
    output: issue
    on_error: auto_heal  # Jira API - may need auth refresh

  # Step 2: Check current status
  - name: check_status
    compute: |
      status = issue.get('status', '').lower()

      # Already done?
      if status == 'done':
        result = {"already_done": True, "message": f"Issue {inputs.issue_key} is already Done."}
      else:
        result = {"already_done": False, "current_status": status}
    output: status_check

  # ==================== GET BRANCH AND COMMITS ====================

  # Step 3: Find branch for this issue (using MCP tool)
  - name: list_branches
    description: "List all git branches"
    condition: "{{ not status_check.already_done }}"
    tool: git_branch_list
    args:
      repo: "{{ inputs.repo }}"
      all_branches: true
    output: branches_raw
    on_error: continue

  - name: find_branch
    description: "Find branch matching this issue using shared parser"
    condition: "{{ not status_check.already_done }}"
    compute: |
      from scripts.common.parsers import parse_git_branches

      issue_key = inputs.issue_key.upper()

      # Use shared parser with issue key filter
      branches = parse_git_branches(branches_raw or "", issue_key=issue_key)

      result = {
        "found": len(branches) > 0,
        "branches": branches,
        "primary": branches[0] if branches else None
      }
    output: branch_info

  # Step 4: Get commits from the branch (using MCP tool)
  - name: get_commit_log
    description: "Get commits from the issue branch"
    condition: "{{ not status_check.already_done and branch_info.found }}"
    tool: git_log
    args:
      repo: "{{ inputs.repo }}"
      limit: 20
      oneline: true
    output: commit_log_raw
    on_error: continue

  - name: parse_commits
    description: "Parse commit log"
    condition: "{{ not status_check.already_done and branch_info.found }}"
    compute: |
      commits = []
      if commit_log_raw:
        for line in str(commit_log_raw).strip().split('\n'):
          if line.strip():
            parts = line.split(' ', 1)
            if len(parts) == 2:
              commits.append({
                "sha": parts[0],
                "message": parts[1]
              })

      result = {
        "branch": branch_info['primary'],
        "base": "main",
        "commits": commits[:20],
        "count": len(commits)
      }
    output: commit_info
    on_error: continue

  # Step 5: Get MR info if exists
  - name: get_mr
    description: "Check for associated Merge Request"
    condition: "{{ not status_check.already_done }}"
    tool: gitlab_mr_list
    args:
      project: "{{ cfg.gitlab_project }}"
      state: "all"
    output: mr_list
    on_error: auto_heal  # GitLab API - may need auth refresh

  # Step 6: Extract MR details
  - name: parse_mr
    condition: "{{ not status_check.already_done and mr_list }}"
    compute: |
      mrs = mr_list if isinstance(mr_list, list) else []

      relevant_mr = None
      for mr in mrs:
        if isinstance(mr, dict):
          title = mr.get('title', '')
          if inputs.issue_key.upper() in title.upper():
            relevant_mr = {
              "id": mr.get('iid'),
              "title": mr.get('title'),
              "state": mr.get('state'),
              "merged": mr.get('state') == 'merged',
              "url": mr.get('web_url')
            }
            break

      result = relevant_mr
    output: mr_info
    on_error: continue

  # ==================== BUILD CLOSING COMMENT ====================

  # Step 7: Build the closing comment
  - name: build_comment
    condition: "{{ not status_check.already_done and inputs.add_comment }}"
    compute: |
      lines = []
      lines.append("h3. ✅ Issue Completed")
      lines.append("")

      # Branch info
      if branch_info.get('found'):
        lines.append(f"*Branch:* {{code}}{branch_info['primary']}{{code}}")

      # MR info
      if mr_info:
        status_emoji = "✅" if mr_info.get('merged') else "🔄"
        lines.append(f"*Merge Request:* [{mr_info['title']}|{mr_info['url']}] {status_emoji}")
        lines.append("")

      # Commits
      if commit_info and commit_info.get('commits'):
        lines.append("h4. Commits")
        lines.append("||SHA||Message||")
        for c in commit_info['commits'][:10]:
          sha_short = c['sha'][:7]
          msg = c['message'][:60].replace('|', '\\|')
          lines.append(f"|{{monospace}}{sha_short}{{monospace}}|{msg}|")

        if commit_info['count'] > 10:
          lines.append(f"_... and {commit_info['count'] - 10} more commits_")
      elif not branch_info.get('found'):
        lines.append("_No branch found for this issue._")
      else:
        lines.append("_No commits found on branch._")

      lines.append("")
      lines.append("----")
      lines.append("_Closed via workflow automation_")

      result = '\n'.join(lines)
    output: closing_comment

  # ==================== UPDATE JIRA ====================

  # Step 8: Add the closing comment
  - name: add_closing_comment
    description: "Add closing comment to Jira"
    condition: "{{ not status_check.already_done and inputs.add_comment and closing_comment }}"
    tool: jira_add_comment
    args:
      issue_key: "{{ inputs.issue_key }}"
      comment: "{{ closing_comment }}"
    output: comment_result
    on_error: auto_heal  # Jira API - may need auth refresh

  # Step 9: Check available transitions
  - name: get_available_transitions
    description: "Check what transitions are available from current status"
    condition: "{{ not status_check.already_done }}"
    tool: jira_get_transitions
    args:
      issue_key: "{{ inputs.issue_key }}"
    output: available_transitions
    on_error: continue

  # Step 10: Validate and select transition
  - name: select_transition
    description: "Select the transition to Done using shared parser"
    condition: "{{ not status_check.already_done }}"
    compute: |
      from scripts.common.parsers import find_transition_name

      target_status = cfg["done_status"]
      transitions_text = str(available_transitions) if available_transitions else ""

      # Look for Done transition in available transitions
      done_available = target_status.lower() in transitions_text.lower()
      transition_name = target_status

      # Use shared parser to find exact transition name
      if not done_available:
          found_name = find_transition_name(transitions_text)
          if found_name:
              done_available = True
              transition_name = found_name

      if not done_available and available_transitions:
          # List what's available
          result = {
              "target_status": target_status,
              "transition_available": False,
              "error": (
                  f"'{target_status}' transition not available from current status. "
                  f"Available: {transitions_text[:200]}"
              ),
          }
      else:
          result = {
              "target_status": transition_name,
              "transition_available": True,
              "error": None,
          }
    output: transition_target

  - name: do_transition
    description: "Execute the transition"
    condition: "{{ not status_check.already_done and transition_target.transition_available }}"
    tool: jira_transition
    args:
      issue_key: "{{ inputs.issue_key }}"
      status: "{{ transition_target.target_status }}"
    output: transition_result
    on_error: continue

  # ==================== VERIFY ====================

  # Step 10: Verify the issue is now Done
  - name: verify_status
    description: "Verify issue status after transition"
    condition: "{{ not status_check.already_done }}"
    tool: jira_get_issue
    args:
      issue_key: "{{ inputs.issue_key }}"
    output: final_issue
    on_error: auto_heal  # Jira API - may need auth refresh

  # Step 11: Emit issue closed hook
  - name: emit_issue_closed_hook
    description: "Notify team channel about closed issue"
    condition: "not status_check.already_done"
    compute: |
      # emit_event is available from skill engine safe_globals
      commit_count = commit_info.get("count", 0) if commit_info else 0

      if emit_event:
          emit_event("issue_closed", {
              "issue_key": inputs.issue_key,
              "commit_count": str(commit_count),
          })
          result = "hook sent"
      else:
          result = "hook skipped: emit_event not available"
    output: close_hook_result
    on_error: continue

  # Notify team about issue closure (skill chain)
  - name: notify_team_issue_closed
    description: "Notify team channel about issue closure"
    condition: "not status_check.already_done and transition_result"
    tool: skill_run
    args:
      skill_name: notify_team
      inputs: |
        {
          "message": "Issue {{ inputs.issue_key }} closed",
          "type": "success",
          "context": "Issue: {{ inputs.issue_key }}\nCommits: {{ commit_info.get('count', 0) if commit_info else 0 }}\nMR Merged: {{ mr_info.get('merged', 'N/A') if mr_info else 'N/A' }}"
        }
      execute: true
    output: slack_issue_closed_notification
    on_error: continue

  # ==================== MEMORY INTEGRATION ====================

  - name: build_memory_context
    description: "Build timestamp for memory"
    condition: "{{ not status_check.already_done }}"
    compute: |
      from datetime import datetime
      result = {"timestamp": datetime.now().isoformat()}
    output: memory_context

  - name: log_session_issue_closed
    description: "Log issue closure to session log"
    condition: "{{ not status_check.already_done }}"
    tool: memory_session_log
    args:
      action: "Closed issue {{ inputs.issue_key }}"
      details: "Commits: {{ commit_info.count if commit_info else 0 }}, MR merged: {{ mr_info.merged if mr_info else 'N/A' }}"
    on_error: continue

  - name: remove_from_active_issues
    description: "Remove issue from active_issues in memory"
    condition: "{{ not status_check.already_done }}"
    compute: |
      # Use shared memory helpers
      issue_key = inputs.issue_key.upper()

      # Remove from active_issues
      memory.remove_from_list("state/current_work", "active_issues", "key", issue_key)

      # Also remove from open_mrs if MR was merged
      if mr_info and mr_info.get("merged"):
          # Find MRs related to this issue and remove them
          data = memory.read_memory("state/current_work")
          open_mrs = data.get("open_mrs", []) if isinstance(data.get("open_mrs"), list) else []
          data["open_mrs"] = [
              m for m in open_mrs
              if m.get("issue_key", "").upper() != issue_key
          ]
          memory.write_memory("state/current_work", data)

      result = "memory updated"
    output: memory_cleanup_result
    on_error: continue

  - name: learn_issue_closure_pattern
    description: "Learn from issue closure for future reference"
    condition: "{{ not status_check.already_done }}"
    compute: |
      from datetime import datetime

      # Load patterns
      patterns = memory.read_memory("learned/patterns") or {}
      if "issue_closures" not in patterns:
          patterns["issue_closures"] = []

      # Record this closure
      closure_record = {
          "issue_key": inputs.issue_key.upper(),
          "branch": branch_info.get("primary") if branch_info else None,
          "commits": commit_info.get("count", 0) if commit_info else 0,
          "mr_merged": mr_info.get("merged", False) if mr_info else False,
          "with_comment": inputs.add_comment,
          "timestamp": datetime.now().isoformat(),
      }

      patterns["issue_closures"].append(closure_record)

      # Keep last 100 closure records
      patterns["issue_closures"] = patterns["issue_closures"][-100:]

      memory.write_memory("learned/patterns", patterns)
      result = "closure pattern learned"
    output: pattern_learn_result
    on_error: continue

  - name: save_shared_context
    description: "Save closure context for other skills"
    condition: "{{ not status_check.already_done }}"
    compute: |
      from datetime import datetime

      # Share context that other skills can use
      shared = memory.read_memory("state/shared_context") or {}

      shared["last_issue_closed"] = {
          "timestamp": datetime.now().isoformat(),
          "issue_key": inputs.issue_key.upper(),
          "branch": branch_info.get("primary") if branch_info else None,
          "commits": commit_info.get("count", 0) if commit_info else 0,
          "mr_merged": mr_info.get("merged", False) if mr_info else False,
      }

      memory.write_memory("state/shared_context", shared)
      result = "shared context saved"
    output: shared_context_result
    on_error: continue

  # ==================== ATTACH SESSION CONTEXT TO JIRA ====================

  - name: attach_session_context
    description: "Attach AI session context to Jira for investigation/audit trail"
    condition: "{{ not status_check.already_done and transition_result }}"
    tool: jira_attach_session
    args:
      issue_key: "{{ inputs.issue_key }}"
      include_transcript: false
    output: session_context_attached
    on_error: continue

  # ==================== LEARNING FROM FAILURES ====================

  - name: detect_close_failures
    description: "Detect failure patterns from close operations"
    compute: |
      errors_detected = []

      # Check Jira API failures
      issue_text = str(issue) if 'issue' in dir() and issue else ""
      transition_text = str(transition_result) if 'transition_result' in dir() and transition_result else ""
      combined = issue_text + transition_text

      if "issue does not exist" in combined.lower():
          errors_detected.append({
              "tool": "jira_view_issue",
              "pattern": "issue does not exist",
              "cause": "Jira issue key is incorrect or issue was deleted",
              "fix": "Verify Jira issue key format (e.g., AAP-12345)"
          })
      if "transition" in combined.lower() and "not available" in combined.lower():
          errors_detected.append({
              "tool": "jira_transition",
              "pattern": "transition not available",
              "cause": "Issue cannot be transitioned to Done from current status",
              "fix": "Check available transitions with jira_view_issue()"
          })

      # Check GitLab failures
      mr_text = str(mr_list) if 'mr_list' in dir() and mr_list else ""
      if "no such host" in mr_text.lower():
          errors_detected.append({
              "tool": "gitlab_mr_list",
              "pattern": "no such host",
              "cause": "VPN not connected - internal GitLab not reachable",
              "fix": "Run vpn_connect() to connect to Red Hat VPN"
          })

      result = errors_detected
    output: close_errors_detected
    on_error: continue

  - name: learn_close_transition_failure
    description: "Learn from transition failures"
    condition: "close_errors_detected and any(e.get('pattern') == 'transition not available' for e in close_errors_detected)"
    tool: learn_tool_fix
    args:
      tool_name: "jira_transition"
      error_pattern: "transition not available"
      root_cause: "Issue cannot be transitioned to Done from current status"
      fix_description: "Check available transitions with jira_view_issue()"
    output: transition_fix_learned
    on_error: continue

outputs:
  - name: summary
    value: |
      {% if status_check.already_done %}
      {{ status_check.message }}
      {% else %}
      ## ✅ Issue Closed: {{ inputs.issue_key }}

      **Status:** {{ final_issue.status if final_issue else 'Done' }}

      {% if branch_info.found %}
      **Branch:** `{{ branch_info.primary }}`
      {% endif %}

      {% if commit_info and commit_info.count > 0 %}
      **Commits:** {{ commit_info.count }}
      {% for c in commit_info.commits[:5] %}
      - `{{ c.sha[:7] }}` {{ c.message[:50] }}
      {% endfor %}
      {% endif %}

      {% if mr_info %}
      **MR:** [{{ mr_info.title }}]({{ mr_info.url }}) - {{ 'Merged ✅' if mr_info.merged else mr_info.state }}
      {% endif %}

      {% if inputs.add_comment %}
      📝 Closing comment added to Jira
      {% endif %}

      🔗 [View Issue]({{ cfg.jira_url }}/browse/{{ inputs.issue_key }})
      {% endif %}

  - name: context
    value:
      issue_key: "{{ inputs.issue_key }}"
      closed: "{{ not status_check.already_done }}"
      branch: "{{ branch_info.primary if branch_info else None }}"
      commits: "{{ commit_info.count if commit_info else 0 }}"
      mr_merged: "{{ mr_info.merged if mr_info else False }}"
